on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

name: ci

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - run: mkdir -p ${{ runner.temp }}/docker

      - name: Build
        uses: docker/build-push-action@v5
        with:
          tags: mdshack/shotshare:ci
          cache-from: type=registry,ref=mdshack/shotshare:latest
          file: ./.docker/Dockerfile
          outputs: type=local,dest=${{ runner.temp }}/docker/image.tar

      - run: chown -R runner:runner ${{ runner.temp }}/docker

      - name: Upload docker image
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: ${{ runner.temp }}/docker

  unit-tests:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download docker image
        uses: actions/download-artifact@v4
        with:
          name: docker-image
          path: ${{ runner.temp }}/docker

      - name: Load docker image
        run: docker load --input ${{ runner.temp }}/docker/image.tar

      - uses: addnab/docker-run-action@v3
        with:
          image: mdshack/shotshare:ci
          run: /app/vendor/bin/pest

  lint:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download docker image
        uses: actions/download-artifact@v4
        with:
          name: docker-image
          path: ${{ runner.temp }}/docker

      - name: Load docker image
        run: docker load --input ${{ runner.temp }}/docker/image.tar

      - uses: addnab/docker-run-action@v3
        with:
          image: mdshack/shotshare:ci
          run: /app/vendor/bin/pint --test

  # static-analysis:
  #   runs-on: ubuntu-latest
  #   steps:
